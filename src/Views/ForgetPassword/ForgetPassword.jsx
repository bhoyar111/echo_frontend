import React from 'react';
import GuestLayout from '../../Container/GuestLayout';
import Form from './Form';

const ForgetPassword = () => {
    return (
        <GuestLayout>
            <Form />
        </GuestLayout>
    );
}

export default ForgetPassword;
